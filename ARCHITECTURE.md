# 🏗️ AI Content Payment & Ownership Tracker - Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                          │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │   Web Browser   │  │   Mobile App    │  │   API Client    │ │
│  │  (React/HTML)   │  │   (Future)      │  │   (Future)      │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                               │
                          HTTP/REST API
                               │
┌─────────────────────────────────────────────────────────────────┐
│                      EXPRESS.JS SERVER                         │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                 API ENDPOINTS                           │   │
│  │  POST /api/generate     GET /api/dashboard/:userId     │   │
│  │  GET /api/content/:id   POST /api/add-funds           │   │
│  │  GET /api/rates         GET /api/proof/:id/:owner     │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                               │
                        Component Layer
                               │
┌─────────────────────────────────────────────────────────────────┐
│                    CORE BUSINESS LOGIC                         │
│                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ X402 Orchestrator│  │   GMPayer       │  │ ERC-8004 Tracker│ │
│  │                 │  │   Payment       │  │   Ownership     │ │
│  │ • Workflow Mgmt │  │ • Balance Mgmt  │  │ • Content Reg   │ │
│  │ • Job Execution │  │ • Transactions  │  │ • Owner History │ │
│  │ • Step Control  │  │ • Rate Config   │  │ • Proof Gen     │ │
│  │ • Error Handle  │  │ • Fund Transfer │  │ • Transfer Mgmt │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
│           │                     │                     │        │
│           └─────────────────────┼─────────────────────┘        │
│                                 │                              │
│  ┌─────────────────┐           │           ┌─────────────────┐ │
│  │ Mock AI Generator│           │           │   Data Layer    │ │
│  │                 │           │           │                 │ │
│  │ • Content Gen   │           │           │ • In-Memory     │ │
│  │ • Model Support │           │           │ • Maps/Arrays   │ │
│  │ • Type Handling │           │           │ • Future: DB    │ │
│  └─────────────────┘           │           └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    Workflow Execution Flow
                                 │
┌─────────────────────────────────────────────────────────────────┐
│                   INTEGRATED WORKFLOW                           │
│                                                                 │
│  User Request → X402 Orchestrator → Multi-Step Process:        │
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │   Step 1    │    │   Step 2    │    │   Step 3    │         │
│  │  Validate   │───▶│   Payment   │───▶│  Generate   │         │
│  │  Request    │    │  (GMPayer)  │    │ (AI Engine) │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
│                                                │                │
│                      ┌─────────────┐          │                │
│                      │   Step 4    │◀─────────┘                │
│                      │  Register   │                           │
│                      │ (ERC-8004)  │                           │
│                      └─────────────┘                           │
│                                                                 │
│  Result: Content + Payment + Ownership → User Dashboard        │
└─────────────────────────────────────────────────────────────────┘
```

## Component Interaction Flow

```
┌─────────────┐
│    User     │
│  Interface  │
└──────┬──────┘
       │ Content Request
       ▼
┌─────────────┐
│    X402     │◀────────────┐
│Orchestrator │             │
└──────┬──────┘             │
       │                    │
       ▼                    │
┌─────────────┐             │
│   GMPayer   │             │
│  (Payment)  │             │
└──────┬──────┘             │
       │                    │
       ▼                    │
┌─────────────┐             │
│  AI Content │             │
│ Generator   │             │
└──────┬──────┘             │
       │                    │
       ▼                    │
┌─────────────┐             │
│ ERC-8004    │             │
│ Tracker     │─────────────┘
│(Ownership)  │
└─────────────┘
```

## Data Flow Architecture

```
INPUT: User Request
{
  userId: "user1",
  contentType: "image-generation",
  prompt: "A futuristic city",
  model: "dall-e-3"
}
         │
         ▼
PROCESSING: X402 Workflow
         │
    ┌────▼────┐
    │Validate │
    └────┬────┘
         │
    ┌────▼────┐
    │ Payment │ ──► GMPayer: Deduct $0.25
    └────┬────┘
         │
    ┌────▼────┐
    │Generate │ ──► AI Engine: Create content
    └────┬────┘
         │
    ┌────▼────┐
    │Register │ ──► ERC-8004: Record ownership
    └────┬────┘
         │
         ▼
OUTPUT: Complete Result
{
  contentId: "content_123",
  paymentTx: "gmp_456",
  ownershipProof: "proof_789",
  content: { ... }
}
```

## Technology Stack

```
Frontend:
├── HTML5 + CSS3 (Modern Grid/Flexbox)
├── Vanilla JavaScript (ES6+)
└── Responsive Design

Backend:
├── Node.js + Express.js
├── RESTful API Design
└── Modular Architecture

Core Components:
├── X402 Orchestrator (Workflow Engine)
├── GMPayer (Payment Processing)
├── ERC-8004 Tracker (Ownership)
└── Mock AI Generator (Content)

Data Layer:
├── In-Memory Storage (Maps/Arrays)
└── Future: Database Integration

DevOps:
├── NPM Package Management
├── Git Version Control
└── Cloud Deployment Ready
```

## Security & Scalability Considerations

```
Security:
├── Input Validation (All API endpoints)
├── Transaction Integrity (Payment atomicity)
├── Ownership Verification (Cryptographic proofs)
└── Error Handling (Graceful failures)

Scalability:
├── Modular Design (Easy component swapping)
├── Database Ready (Replace in-memory storage)
├── API First (Multiple frontend support)
└── Cloud Native (Containerization ready)

Future Enhancements:
├── Real AI Integration (OpenAI, Anthropic APIs)
├── Blockchain Integration (Smart contracts)
├── User Authentication (JWT, OAuth)
└── Advanced Analytics (Usage patterns)
```
